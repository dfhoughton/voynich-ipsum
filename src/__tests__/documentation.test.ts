/** tests for all the code shown in documentation */

// yes, this is shameful, but what do you expect for a gibberish generator?

describe('README', () => {
  describe('Synopsis', () => {
    test('stubbed', () => expect(true).toEqual(true))
  })//
})

describe('index.ts', () => {
  describe('something', () => {
    test('stubbed', () => expect(true).toEqual(true))
  })
})
